{
  "rules": [
    {
      "allowed": {
        "origins": [
          "http://localhost:3000",
          "https://resonance-world.vercel.app"
        ],
        "methods": ["GET", "PUT", "HEAD"],
        "headers": ["*"]
      },
      "exposeHeaders": ["ETag"],
      "maxAgeSeconds": 3600
    }
  ]
}
